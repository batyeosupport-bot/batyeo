export interface FeatureFlags {PAYMENT_PROVIDER:'mock'|'stripe_test';MANUFACTURER_PROVIDER:'disabled'|'bajie';PHYSICAL_ACTIONS_ENABLED:boolean;STATION_RUNTIME_ENABLED:boolean;SCHEDULER_ENABLED:boolean;SIMULATOR_ENABLED:boolean;DEMO_MODE:boolean;}
export function readFeatureFlags(env:Record<string,string|undefined>):FeatureFlags{const payment=(env.PAYMENT_PROVIDER||'mock') as FeatureFlags['PAYMENT_PROVIDER'];const manufacturer=(env.MANUFACTURER_PROVIDER||'disabled') as FeatureFlags['MANUFACTURER_PROVIDER'];const physical=env.PHYSICAL_ACTIONS_ENABLED==='true'||env.MANUFACTURER_ALLOW_PHYSICAL_ACTIONS==='true';const production=env.NODE_ENV==='production';if(!['mock','stripe_test'].includes(payment)||payment==='stripe_test'&&(!env.STRIPE_SECRET_KEY?.startsWith('sk_test_')))throw new Error('Invalid or live payment configuration.');if(!['disabled','bajie'].includes(manufacturer))throw new Error('Invalid manufacturer provider.');if(manufacturer==='bajie'&&(!env.MANUFACTURER_API_BASE_URL||!env.MANUFACTURER_USERNAME||!env.MANUFACTURER_PASSWORD))throw new Error('Bajie configuration is incomplete.');if(physical)throw new Error('Physical actions are disabled in this phase.');if(env.DEMO_MODE==='true'&&production)throw new Error('Demo mode cannot run in production.');if(env.SIMULATOR_ENABLED==='true'&&production)throw new Error('Simulator cannot run in production.');if(env.SCHEDULER_ENABLED==='true'&&!env.SCHEDULER_SECRET)throw new Error('Scheduler secret is required when scheduler is enabled.');return {PAYMENT_PROVIDER:payment,MANUFACTURER_PROVIDER:manufacturer,PHYSICAL_ACTIONS_ENABLED:false,STATION_RUNTIME_ENABLED:env.STATION_RUNTIME_ENABLED!=='false',SCHEDULER_ENABLED:env.SCHEDULER_ENABLED==='true',SIMULATOR_ENABLED:env.SIMULATOR_ENABLED==='true',DEMO_MODE:env.DEMO_MODE==='true'};}

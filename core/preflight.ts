import type {ProviderHealth} from './types';
export type PreflightResult='PASS'|'FAIL'|'UNKNOWN';
export interface HardwarePreflightInput {environment:string;physicalActionsEnabled:boolean;stationAllowlisted:boolean;operatorConfirmed:boolean;coreHealthy:boolean|undefined;dbHealthy:boolean|undefined;providerHealth:ProviderHealth;stationOnline:boolean|undefined;availableBatteries:number|undefined;conflictingRental:boolean;criticalIncidents:number;commandId:string;}
export function preflightHardwareCommand(input:HardwarePreflightInput){const unknown:string[]=[];const failures:string[]=[];if(input.environment!=='staging')failures.push('staging-only');if(!input.physicalActionsEnabled)failures.push('physical-actions-disabled');if(!input.stationAllowlisted)failures.push('station-not-allowlisted');if(!input.operatorConfirmed)failures.push('operator-confirmation-required');if(input.coreHealthy===undefined)unknown.push('core-health');else if(!input.coreHealthy)failures.push('core-unhealthy');if(input.dbHealthy===undefined)unknown.push('db-health');else if(!input.dbHealthy)failures.push('db-unhealthy');if(input.providerHealth==='UNKNOWN')unknown.push('provider-health');else if(input.providerHealth!=='HEALTHY')failures.push('provider-unhealthy');if(input.stationOnline===undefined)unknown.push('station-status');else if(!input.stationOnline)failures.push('station-offline');if(input.availableBatteries===undefined)unknown.push('battery-availability');else if(input.availableBatteries<1)failures.push('no-battery');if(input.conflictingRental)failures.push('conflicting-rental');if(input.criticalIncidents>0)failures.push('critical-incidents');if(!input.commandId)failures.push('missing-command-id');const result:PreflightResult=failures.length?'FAIL':unknown.length?'UNKNOWN':'PASS';return {result,failures,unknown};}

# Rollback

Disable scheduler and physical actions, keep the last-known-good runtime config, and revert only through an reviewed migration/release rollback. Re-run typecheck, tests, migration status and reconciliation before reopening traffic.

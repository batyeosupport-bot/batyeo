# Provider down

Confirm provider health and last successful read-only sync. Keep rentals and physical actions blocked, do not retry writes, and open a reconciliation incident. Restore connectivity, run a manual read-only sync, then resume the scheduler.
